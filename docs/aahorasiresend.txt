¡Excelente! He actualizado el código para que el enlace del correo te mande directo a la administración.

### PASO FINAL PARA LA REDIRECCIÓN:

Para que Supabase permita que el correo mande al usuario a `/admin.html`, debes registrar esa dirección en tu panel de control:

1. Ve a **Authentication** -> **URL Configuration**.
2. En la sección **Redirect URLs**, haz clic en **Add URL**.
3. Pega exactamente esto: `https://tragalero.com/admin.html`
4. Dale a **Save**.

### Cambios en el código:
He modificado el archivo `login.html` para que, al momento del registro, le pida a Supabase que el botón del correo redirija a la página de administración.

¡Con esto ya tienes el flujo perfecto de principio a fin!
